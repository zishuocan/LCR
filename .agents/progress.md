# LCR_G474 开发进度

最后更新：2026-07-19

## 进度记录规则

- 每完成一个可独立验收的小改动，更新本文件。
- 记录编译结果、上板结果、已知问题和下一步，不用口头状态替代记录。
- 未经实测的功能标记为“待验证”，不写成已完成。

## 最终验收基线（后续增量开发不得回退）

- 电阻10 Ω～100 kΩ、电容1 nF～4.7 µF、电感10 µH～100 µH三类功能必须同时保留；最终每类准备并测试不少于7个元件，不以单个电容测通代替完整验收。
- 一键启动后自动完成测量；测量频率、激励幅度和串/并联等效方式须与商用参考仪器一致。
- 所有验收点相对误差绝对值不大于5%，元件真值以商用LCR表测量结果为准，当前万用表电阻值只能用于开发期检查。
- 最终显示必须同时包含`Z=R±jX`、元件类型和对应R/C/L参数；电容显示ESR，电感显示串联电阻，并保留频率信息。
- 保持自平衡电桥实现，不使用AD5933等专用阻抗测量芯片；新增电容/电感路径不得删除或旁路已经完成的电阻、自动量程、异常透明记录和错误状态功能。
- 当前仍是逐阶段开发：已有10 Ω量级、100 Ω量级、1 kΩ量级和10 kΩ量级电阻检查点，不代表最终三类各7个元件及商用LCR表校准已经完成。

## 已完成

### 1. 项目与硬件核对

- 已读取验收要求、开发计划、原理图和 PCB 图。
- 已确认 PA4/DAC1_OUT1 -> LCR 板 J3/SINE。
- 已确认 PA0/ADC1_IN1 <- J1/V_AMP。
- 已确认 PA1/ADC2_IN2 <- J2/I_AMP。
- 已确认 VGA_CS1 控制电压通道，VGA_CS2 控制电流通道。
- 已确认 GAIN_A0~A2、VGA_CS1、VGA_CS2 为 5 V 上拉，MCU 使用开漏输出。

### 2. DAC 激励模块

- 新增 `lcr_excitation.c/.h`。
- 实现 64 点正弦表、幅度设置、TIM6 频率设置以及 DAC DMA 安全启停。
- 默认参数：10 kHz、幅度 1200 DAC counts、中心码 2048。
- 上电不启动 DAC；PC13 中断只置标志，主循环执行启停和串口输出。

### 3. 反馈量程与 PGA

- 新增 `lcr_range.c/.h`。
- ADG1609 映射：00=100 kOhm//2.2 nF，01=10 kOhm//220 pF，10=1 kOhm//22 pF，11=100 Ohm//4.7 pF。
- AD8231 增益支持 1/2/4/8/16/32/64/128。
- 安全启动状态：反馈 100 Ohm，V 增益 1，I 增益 1。

### 4. 构建与首次上板

- Debug 与 Release 均全量编译通过，无编译警告。
- Release 占用：FLASH 19724 B，RAM 2872 B。
- 已通过 ST-LINK 向 NUCLEO-G474RE 烧录 Debug 固件并完成 Flash 校验。
- 已通过 COM23 验证安全启动日志。
- 已收到一次 PC13 启动日志：`excitation started: 10000 Hz, amplitude=1200 counts`。

## PA4 无波形问题（已修复并完成原始 DAC 波形确认）

- 示波器在 Arduino A2/PA4 未观察到波形。
- 2026-07-19 最新 SWD 寄存器快照显示激励当时已停止：
  - TIM6 CR1 = 0x00000080，CEN=0。
  - DMA1 Channel 1 CCR = 0x000025B0，EN=0。
  - DAC1 CR = 0x0000001E，EN1=0。
- 初次寄存器快照是在停止状态取得，随后通过清空串口缓冲、复位并重新触发，稳定复现了 DMA 故障。

### DAC DMA 根因定位

- 在确定收到 `excitation started` 后再次读取运行态寄存器：TIM6 CEN=1、DAC1 EN1=1，但 DMA1 Channel 1 EN=0，DAC1 DOR1 保持 2048，DAC1 SR 的 DMAUDR1=1。
- DMA 源地址、32 点波形数据、目标寄存器地址和 DMAMUX 请求号均正确。
- 对照 STM32CubeG4 V1.6.3 官方 DAC 示例，DAC DMA 应配置为“外设端 Word、内存端 Half Word”。原工程的外设端 Half Word 会立即触发 DMA transfer error。
- 已同步修正 `LCR.ioc` 和 `Core/Src/dac.c`。
- 修正版已全量构建、烧录并完成 Flash 校验。
- 通过 SWD 触发 EXTI13 后收到新的 `excitation started` 日志。
- 修复后运行态寄存器：
  - TIM6 CR1 = 0x00000081，CEN=1，ARR=499。
  - DMA1 Channel 1 CCR = 0x000026BF，EN=1、Circular=1；CNDTR 正在循环变化。
  - DAC1 DHR12R1 和 DOR1 正在取正弦表中的不同值。
  - DAC1 SR 不再出现 DMAUDR1，DMA ISR 无错误标志。
- 软件和外设运行链路已验证通过。
- 示波器已在 Arduino A2/PA4 观察到 32 点阶梯正弦：约 10 kHz、平均值 1.5903 V、峰峰值 1.9551 V。
- 实测幅度与目标中心约 1.65 V、峰峰值约 1.94 V 基本一致；可见台阶是 32 点 DAC 零阶保持的正常原始波形。
- LCR 模拟板在 SINE 输入后配置了两级约 40.8 kHz 的 RC 低通和 U1A 缓冲，实际 DUT 激励质量需在滤波后节点确认。

### 滤波后波形与 64 点升级

- 已在滤波后节点观察到约 10 kHz、1.4564 Vpp 的连续正弦，32 点台阶已被滤除，但波峰/波谷仍有轻微不对称和钝化。
- 初看示波器截图时曾怀疑 CH1 为 AC 耦合；结合后续重复出现的约 1.153 V 平均值和 J2 直流偏置，现将该值作为供电诊断线索，最终以直接测量模拟板电源轨为准。
- 10 kHz 基准档已由 32 点升级为 64 点，DAC/TIM6 同步触发率由 320 kS/s 提高到 640 kS/s。
- 双 ADC 当前 40 MHz ADC 时钟、24.5 周期采样时间，单次 12 位转换约需 37 个 ADC 周期，理论上限约 1.08 MS/s；640 kS/s 留有转换余量。
- 1024 点采集窗口在 64 点/周期下仍恰好包含 16 个周期，不会破坏后续单频 DFT 的整周期条件。
- 增加 800 kS/s 同步触发率上限；高频测量档后续必须切换为较短正弦表，不能直接沿用 64 点表。
- 64 点 Debug 固件已全量编译通过、烧录并完成 Flash 校验。
- 64 点版本上板运行态：TIM6 CR1=0x00000081、ARR=249；DMA1 Channel 1 CCR=0x000026BF，CNDTR 在 32 和 8 等值间循环变化；DAC1 DOR1 在不同波形码间变化，DAC1 SR 的 DMAUDR1=0。
- 64 点版本的软件与外设链路已验证。
- 64 点版本滤波后复测约为 1.4564 Vpp、平均值 1.1537 V；与 32 点版本的 1.4564 Vpp、平均值 1.1532 V 基本相同，肉眼波形改善不明显。
- 由对比结果判断，滤波后可见形状并非主要由 32 点 DAC 台阶造成；不再盲目增加正弦表点数，保留 64 点以便后续 1024 点窗口恰好采集 16 周期。
- 当前波形连续、周期稳定且未见明显硬削顶；是否影响阻抗测量将在双 ADC 实采后通过基波幅相和谐波比定量验证。

### 5. 双 ADC 同步采集（采集链路已运行，I 通道输入待排查）

- 新增 `lcr_capture.c/.h`，使用 `HAL_ADCEx_MultiModeStart_DMA()` 一次采集 1024 组 32 位双 ADC 数据。
- ADC1、ADC2 在启动前均完成单端校准；DMA 完成和错误回调只更新采集状态，统计与串口输出均在主循环执行。
- 根据 STM32G474 CMSIS 的 ADC12 公共 CDR 定义确认：低 16 位 `RDATA_MST` 为主 ADC1/PA0/V_AMP，高 16 位 `RDATA_SLV` 为从 ADC2/PA1/I_AMP。
- 增加每通道 first、last、min、max、mean、p2p 和近电源轨样本数统计；按键启动激励后等待 10 ms，再自动采集 1024 组数据。
- Debug 全量编译通过且无警告；RAM 7064 B，FLASH 36004 B。固件已烧录并通过 Flash 校验。
- 第一次实采成功收满 1024 组：
  - V/ADC1/PA0：min=212，max=1964，mean=1029，p2p=1752，rail=0。
  - I/ADC2/PA1：min=0，max=1055，mean=5，p2p=1055，rail=1006；原始数据仅开始约 18 点衰减，随后为 0。
- 再次停止并重新启动后复测：
  - V/ADC1/PA0：min=233，max=1976，mean=1041，p2p=1743，rail=0。
  - I/ADC2/PA1：1024 点全部为 0。
- SWD 已确认 ADC2 的 SQR1 选择 Channel 2、GPIO PA1 为模拟模式；DMA 原始缓冲区高半字与 ADC2 DR 均为 0。同步 DMA 本身已经收满，当前需优先确认 LCR 板 J2/I_AMP 到 NUCLEO A1/PA1 的接线和实际直流电压。
- 用户调整接线后再次实采，I/ADC2/PA1 仍为 1024 点全 0；V/ADC1/PA0 为 min=216、max=1962、mean=1028、p2p=1746、rail=0。
- SWD 读取 GPIOB 确认 PB4/VGA_CS2 的 ODR 和 IDR 均为高电平，电流通道 PGA 未被 MCU 保持在关断状态；仍需直接测量 J2 与 A1 电压。
- 用户在 LCR 板 J2/I_AMP 实测约 0.78 V 直流，仅有很小的交流抖动。无 DUT 电流时 J2 没有明显正弦本身可以正常，但其直流偏置暴露了供电异常。
- 原理图中 VCOM 由 +5 V 的 100 kΩ/100 kΩ 分压产生，滤波后节点平均值约 1.153 V 对应电源约 2.31 V；I_AMP 由 2 kΩ/1 kΩ 分压偏置，J2 约 0.78 V 对应电源约 2.34 V。两条独立证据一致指向模拟板名义 +5 V 电源实际约为 2.3 V。
- 在确认模拟板 J7 的 +5 V 电源轨实际达到约 5.0 V 前暂停算法开发；该欠压也可能是此前正弦轻微失真和 ADC 电平异常的共同原因。
- 供电/接线再次调整后复测仍未变化：V/ADC1/PA0 为 min=208、max=1958、mean=1024、p2p=1750、rail=0；I/ADC2/PA1 仍为 1024 点全 0。必须取得 J7 电源和 A1 引脚的直接电压读数后再继续。
- 最新一次接线调整已生效：V/ADC1/PA0 为 min=227、max=1971、mean=1040、p2p=1744、rail=0；I/ADC2/PA1 为 min=1024、max=1071、mean=1039、p2p=47、rail=0。J2→A1/PA1 信号链和双 ADC 打包均已实际验证，不再存在 I 通道全零问题。
- 当前 I 通道只有约 47 counts 峰峰值；若未接 DUT，这是零电流附近的噪声/残余耦合，符合预期。若已接 DUT，则还需检查 DUT 与 Kelvin 端连接。
- 随后确认上述约 1040 counts 的采集发生在模拟板供电断开/异常期间，不能作为正常供电验收数据。
- 恢复模拟板供电后重新实采：
  - V/ADC1/PA0：min=1238、max=3034、mean=2085、p2p=1796、rail=0，折算中心约 1.68 V、峰峰值约 1.45 V。
  - I/ADC2/PA1：min=2059、max=2107、mean=2085、p2p=48、rail=0，折算中心约 1.68 V、峰峰值约 39 mV。
- 正常供电数据与原理图的 +5 V 经 2 kΩ/1 kΩ 分压产生约 1.67 V 偏置一致；V 通道无削顶，I 通道在未接 DUT 时仅有小幅噪声。供电、J2→A1 接线和 1024 点双 ADC 同步采集链路正式验证通过。

### 6. 单频 DFT 与谐波分析

- 新增 `lcr_dsp.c/.h`，使用与激励同步的 64 点 Q15 正余弦表，对 1024 点（16 个完整周期）计算 1～5 次谐波。
- 输出基波复数实部/虚部、幅值、相位、V-I 相位差、各次谐波相对基波比例及 THD2-5；基波低于 5 ADC counts 时标记相位无效，避免输出噪声相位。
- DFT、平方根和相位计算只在 DMA 完成后的主循环执行，中断仍只置完成/错误状态。
- 理想 64 点、900 counts 幅值的离线自检结果：基波 899.945 counts，THD2-5=0.0153%，验证 DFT 缩放和谐波索引正确。
- Debug 全量编译通过且无警告；RAM 7072 B，FLASH 40096 B。固件已烧录并完成 Flash 校验。
- 正常供电、未接 DUT 的板上结果：V 基波 904.651 counts，相位 -7.056°，THD2-5=7.551%（H2=6.531%、H3=3.523%、H4=1.360%、H5=0.317%）；I 基波低于 5 counts并被正确判为无效。
- 当前开路/未接 DUT 时感测端工况不代表实际阻抗测量，V 通道谐波数据先作为基线记录；需接入已知电阻后同时评估 V/I 基波及相位差。
- 用户接入 100 kΩ DUT 后，将反馈网络从安全启动的 100 Ω 档切换到 100 kΩ//2.2 nF 档，PGA 维持 1 倍；Debug 固件重新全量编译、烧录并校验通过。
- 100 kΩ DUT 实采：V min=1250、max=3039、mean=2090、p2p=1789；I min=2000、max=2180、mean=2090、p2p=180；两路均无削顶。
- DFT 结果：V H1=903.097 counts、phase=-63.244°、THD2-5=7.662%；I H1=75.121 counts、phase=25.582°、THD2-5=3.797%；原始 V-I phase=-88.826°。
- I 基波约 75 counts，证明 100 kΩ DUT 在 100 kΩ反馈档可以稳定测量。原始约 -89° 相位主要由 100 kΩ//2.2 nF 反馈阻抗及反相跨阻关系产生，复阻抗换算必须使用复数反馈阻抗，不能直接把 V-I 相位当作 DUT 相位。

### 7. 首个未经校准的复阻抗

- 新增 `lcr_impedance.c/.h`，按 `Zdut = -(Gi/Gv) * Zf * (V/I)` 计算，其中 `Zf = Rf/(1+jωRfCf)`，同时补偿反相跨阻、PGA增益比和反馈电容相移。
- Debug 全量编译通过且无警告；RAM 7072 B，FLASH 41400 B。固件已烧录并完成 Flash 校验。
- 100 kΩ档再次实采得到 V H1=903.864 counts、I H1=76.133 counts、V-I=-88.698°；板上原始阻抗输出为 `85277.584 + j8120.124 Ω`，|Z|=85663.312 Ω、phase=5.439°。
- 用户用万用表测得该 DUT 实际为 93.7 kΩ，因此相对实际值：原始电阻分量误差约 -8.99%，幅值误差约 -8.58%，固定相位误差约 +5.439°。
- 由 93.7 kΩ 参考值计算的当前档位复数校准系数为 `K = 1.088892 - j0.103684`，|K|=1.093817、phase=-5.439°。该系数只适用于 10 kHz、100 kΩ反馈档、当前增益与硬件链路。

### 8. 单档复数校准（接口完成，系数待稳定连接后重取）

- 新增 `lcr_calibration.c/.h`，按频率、反馈档、V PGA增益和I PGA增益精确匹配复数校准点；未匹配的档位拒绝套用系数。
- 已将首个临时系数接入阻抗结果；Debug 全量编译通过且无警告，RAM 7072 B、FLASH 42364 B，固件已烧录并完成校验。
- 独立复测一度得到 raw `98156.088 + j12473.566 Ω`，套用临时系数后为 `108174.680 + j3405.130 Ω`，与93.7 kΩ参考值偏差明显，说明单次参考采样不具备足够重复性。
- 随后连续4次采集的I/ADC2均固定为2100 counts、p2p=0，基波低于5 counts；V通道仍稳定在约905 counts基波。该特征明确表示DUT电流路径开路/接触丢失，而非随机噪声或DFT误差。
- 因参考件连接不稳定，当前系数仅保留为临时实现验证值，不作为有效校准数据；必须恢复稳定的四线DUT连接后连续采样，再重算系数。
- 用户再次调整后连续5次复测，I通道仍全部固定为2100 counts、p2p=0，而V基波稳定在约904～906 counts；确认当前J6仍未形成DUT+到DUT-的电流回路。PCB/原理图复核J6顺序为1=DUT+、2=DUT+SENSE、3=DUT-SENSE、4=DUT-。
- 进一步测试发现“复位后第一次I通道正常、停止后再次采集ADC2恒定”的规律，推翻了单纯接触不良结论；根因是双ADC从机在第二次采集前未通过真实`hadc2`句柄重新武装。
- `lcr_capture.c`已修正为每次先`HAL_ADC_Start(&hadc2)`使能双模从机，再调用`HAL_ADCEx_MultiModeStart_DMA(&hadc1, ...)`；结束时同时整理主从ADC状态。修正版全量编译、烧录和Flash校验通过。
- 修复后连续5次启动/停止均获得有效I基波，不再出现ADC2固定值，证明重复采集故障已解决。
- 5次中I通道THD低于5%的3次校准结果：93.540 kΩ、93.974 kΩ、93.721 kΩ；平均93.745 kΩ，相对万用表93.700 kΩ误差+0.048%，样本标准差约0.218 kΩ，极差约0.463%。
- 另2次I通道THD分别约7.7%和16.1%，对应97.673 kΩ和107.586 kΩ离群结果。必须增加采样质量门限并对多个合格结果平均，不能直接显示单次结果。

### 9. 采样质量结果必须透明显示

- 质量门限只决定样本是否参与最终平均，不允许静默删除或隐藏异常样本。
- 每次采样仍输出 ADC 摘要、基波、THD、原始阻抗和校准阻抗；同时明确标注 `VALID` 或 `SUSPECT`、`retained=yes`、是否参与平均以及全部判定原因。
- 当前判定原因包括 V/I 近电源轨、V/I 基波无效、V THD 超过10%、I THD超过5%，超限项同时输出实际数值和门限。
- Debug 全量编译通过且无警告，RAM 7072 B、FLASH 43864 B；固件已烧录并通过 Flash 校验。
- 上板先获得一条正常样本：`quality=VALID; retained=yes; eligible_for_average=yes`，校准结果 `93686.808-j60.503 Ω`。
- 随后连续8次验证同时覆盖两类输出：2次 `VALID`；6次 `SUSPECT` 均保留完整阻抗结果，并逐条输出 `I_THD=实际值%>5.000%`。例如13.926%样本仍输出校准结果 `104426.672+j1867.541 Ω`，验证异常结果没有被静默丢弃。
- 每次验证后均已停止激励，当前输出为停止状态。

### 10. I通道THD漂移与PGA诊断

- 分别保存并分析了一组 `VALID` 和一组 `SUSPECT` 的1024点原始双ADC数据。两组V通道均约905 counts、THD约7.5%；I通道分别为76.884 counts/4.309%和66.263 counts/16.269%。
- 16个逐周期DFT显示每个采集窗口内部的I幅值和相位稳定，原始缓冲区没有丢点或主从错位；异常是真实的I基波下降及二次谐波上升，不是DFT计算伪影。
- 保持DAC连续运行、只重复启动ADC的10次对照仍出现THD约4.4%～16.4%的变化，排除了反复启停正弦造成的瞬态。
- 直接扫描电流PGA时，1×仍有约74～75 counts基波；切到2×、4×、8×或16×后反而只剩约36～56 counts峰峰值噪声，基波低于5 counts。该现象不符合AD8231增益真值表。
- 对共享地址线做电压PGA交叉测试：U4从1×切到2×后，V基波由904.740增加到1804.038 counts，证明A0～A2编码、共享地址总线和U4控制正确；故障被收敛到U5电流PGA通道或其输入直流工作点/CS2连接。
- 由于J2位于U5输出后的C9交流耦合与重新偏置之后，J2约1.67 V平均值无法反映U5A输出是否已经靠近0/5 V电源轨。下一步必须测量U5前级直流工作点，才能区分DUT负端Sense接法、U5输入直流失配和U5器件/CS2故障。
- 用户提供PCB局部图后重新确认C9位置：C9是U5右下方、R11/R12下方的横向1 uF耦合电容；靠U5的焊盘为U5A输出侧，靠R11/R12的焊盘为后级偏置侧。此前总览图中对C9附近去耦区域的口头定位作废。
- 板上直流实测：J6的DUT-_SENSE与DUT-差值几乎为0 V，DUT-对GND为2.45 V；C9两端分别为1.65 V和0.058 V。1.65 V符合R11/R12产生的后级偏置，0.058 V对应U5A输出侧贴近下电源轨，能够解释电流PGA升到2×后交流基波消失以及1×时THD波动。
- 修复ADC2重复启动后重新扫描反馈MUX：code00约69～73 counts/THD 7.3%～12.0%，code01稳定约91.9 counts/THD约8.0%，code10约6.75 counts/THD约18%～19%，code11基波无效；MUX地址变化确实会改变交流响应，但没有消除U5A低轨工作点问题。
- 直接测量 `TIA_OUT`：code00时仅0.36 V，切到code01后恢复2.49 V；结合DUT-/VCOM约2.45 V，确认U2、U5及公共控制链路可工作，故障集中在code00对应的100 kΩ反馈支路（R8/C7、U3 S1A/S1B触点或相关焊接/走线）。
- 保持code01后复测电流PGA：1×/2×/4×/8×/16×基波依次为91.516/186.016/375.082/753.755/1509.348 counts，均无削顶，THD稳定约7.4%；证明U5增益控制正常，先前高增益无信号是由code00下TIA直流饱和造成。
- 当前固定测量配置已改为10 kΩ反馈、V增益1×、I增益8×；I THD门限由5%修正为10%，因为健康电阻通道会包含激励本身约7.5%的谐波，固定5%会把正常样本误标为异常。异常样本仍完整输出且不静默丢弃。
- 新配置连续8次均为 `VALID`：I基波751.795～752.311 counts、无削顶；raw R平均95301.076 Ω、样本标准差33.566 Ω，raw X平均-482.744 Ω，重复性显著恢复。
- 以万用表93.7 kΩ为临时参考生成新配置复数系数 `K=0.9831746+j0.0049802`；旧code00/100 kΩ故障支路的临时系数已移除。Debug全量编译无警告，RAM 7072 B、FLASH 43932 B，烧录和Flash校验通过。
- 新系数烧录后5次独立结果全部标为 `VALID`，校准R为93.014～94.851 kΩ，平均93.926 kΩ，相对93.700 kΩ为+0.242%；每次原始和校准复阻抗均完整输出。结果仍显示单次相位/虚部会波动，下一步必须做多次复数平均并报告全部样本。
- 当前硬件保持10 kΩ反馈、V增益1×、I增益8×，激励停止；临时原始数据文件已清理。
- 用户要求再次确认100 kΩ反馈档后完成同条件A/B复测：code00连续3次I通道仅32/48/42 counts峰峰值，基波均低于5 counts并标记 `SUSPECT`；随即切回code01连续3次I基波为749.471/749.341/749.526 counts、THD约7.56%且全部 `VALID`。确认code00故障可重复，不是偶发采样问题；测试后已恢复code01并停止激励。
- 按用户要求继续正常开发并临时替代坏档：量程模块新增“请求档/实际档/回退状态”和档位可用性表；100 kΩ反馈档保留但标记为隔离不可用，所有正常测量请求该档时自动实际选择10 kΩ反馈，直接硬件诊断接口仍可显式切换code00。
- 回退不是静默进行：启动串口实测输出 `feedback fallback: requested=100000 ohm is quarantined, actual=10000 ohm`；阻抗计算与校准始终读取实际10 kΩ档，避免套用100 kΩ参数。修好硬件后只需解除可用性隔离并重新建立对应校准点。
- 回退策略Debug全量编译无警告，RAM 7072 B、FLASH 44244 B，固件烧录和Flash校验通过；上板单次结果 `93560.248-j148.141 Ω`、质量 `VALID`。测试后激励停止。

### 11. 一键8次测量与透明复数平均

- 新增 `lcr_measurement.c/.h` 会话模块；单次按键启动后自动完成8次1024点双ADC采集，每次继续输出完整ADC摘要、DFT、质量原因、raw Z和calibrated Z。
- 仅 `VALID` 且阻抗计算成功的结果进入复数R/X累加；`SUSPECT` 结果仍逐条输出并计入 `ineligible`，不静默删除。汇总明确输出attempts、eligible、ineligible和平均值来源（raw/calibrated）。
- 8次结束后对R和X分别求平均，再由平均复数计算幅值和相位；激励自动停止并回到等待按键状态。测量中再次按键可中止，并报告已完成次数。
- Debug全量编译无警告，RAM 7104 B、FLASH 46468 B；固件已烧录并通过Flash校验。
- 正常路径实测一次触发完成8次，8条均明确输出 `VALID; retained=yes; eligible_for_average=yes`；最终汇总 `attempts=8 eligible=8 ineligible=0`，平均校准阻抗 `93594.567-j179.444 Ω`，随后自动停止激励。
- 使用已知坏的code00做故障注入：8次均逐条输出 `SUSPECT; retained=yes; eligible_for_average=no` 和原因 `I_H1_INVALID`；最终汇总 `attempts=8 eligible=0 ineligible=8`、`average unavailable`，验证异常样本没有被隐藏或伪造平均值。
- 故障注入后已恢复code01实际10 kΩ反馈和I增益8×；GPIO状态与TIM6停止状态经SWD复核正确。

### 12. 第二只约1 kΩ电阻盲测

- 为避免10 kΩ反馈/I增益8×在约1 kΩ DUT下削顶，盲测配置切换为健康的1 kΩ反馈、V增益1×、I增益1×；启动日志确认实际反馈为1000 Ω，未触发100 kΩ坏档回退。
- 当前10 kHz/1 kΩ反馈/V1×/I1×组合尚无校准点，因此本次只报告原始复阻抗，不借用93.7 kΩ参考件建立的10 kΩ反馈/I8×校准系数。
- 第一轮8次全部 `VALID`，平均原始阻抗为 `1216.165+j10.503 Ω`，幅值1216.211 Ω，相位+0.495°；电流ADC峰峰值约1355～1364 counts，无削顶。
- 第二轮独立8次也全部 `VALID`，平均原始阻抗为 `1216.317+j10.282 Ω`，幅值1216.361 Ω，相位+0.484°；两轮电阻分量仅相差0.152 Ω（约0.0125%）。
- 16次合并盲测中心值为约 `1216.241+j10.393 Ω`。用户尚未公布电阻的万用表/LCR参考值，待公开后再计算绝对误差；两轮结束后激励均已自动停止。
- 用户随后公布万用表参考值为1.194 kΩ；盲测电阻分量偏高22.241 Ω，相对误差+1.863%。16次重复性远小于绝对误差，确认该档主要是尚未校准的系统增益/相位误差，而不是随机漂移。

### 13. 自动量程状态机

- 新增 `lcr_autorange.c/.h`。每次一键测量从100 Ω反馈、V增益1×、I增益1×和默认激励幅度开始，依据V/I两路ADC交流峰峰值自动调整两个PGA、可用反馈档及必要时的DAC激励幅度。
- 当前锁定窗口为ADC峰峰值820～3276 counts（约满量程20%～80%）；相比最初10%下限留出更多THD/SNR余量。每次换档后重新等待10 ms再采样，最多调整12次，禁止无限循环。
- 电流灵敏度不是硬编码单一路径：算法动态枚举“实际可用反馈档×PGA增益”并选择更合适组合；已隔离的100 kΩ反馈档自动跳过且启动日志明确报告。若最低电流灵敏度仍削顶，则自动降低激励幅度，最低允许150 DAC counts，以覆盖后续低阻件测试。
- 自动量程试探样本全部保留并输出ADC、DFT、质量和原始阻抗，但明确标记 `eligible_for_average=no; reason=AUTORANGE_PROBE`；锁定量程后重新开始8次正式采集，不把不同量程的试探结果混入平均。
- 第一版按10%下限在1.194 kΩ DUT上3次试探后锁定100 Ω反馈/I4×；第一轮8/8有效，平均 `1228.133+j43.309 Ω`，第二轮因I THD=10.080%出现1条明确保留的SUSPECT样本，证明下限余量不足。
- 将锁定下限提高到20%后重新全量构建、烧录并通过Flash校验；RAM 7120 B，FLASH 51528 B。上板仍用3次试探锁定100 Ω反馈/V1×/I8×，I峰峰值约928～937 counts，I THD降至9.413%～9.502%，正式8次全部VALID。
- 改进版平均原始阻抗为 `1210.557+j40.206 Ω`，电阻分量相对1.194 kΩ参考值误差+1.387%，满足当前±5%验收指标；测量后激励自动停止。

### 14. 基于复数平均的R/C/L初步判别

- 新增 `lcr_component.c/.h`，输入必须是完整测量会话得到的8次复阻抗平均值和实际测试频率；不根据单个采样结果直接判别元件。
- 当前规则：平均相位绝对值不超过5°判为电阻；相位小于等于-10°且X<0判为电容；相位大于等于+10°且X>0判为电感；5°～10°模糊带保持UNKNOWN并明确要求多频复测，不强行猜测。
- 电容按 `C=-1/(2πfX)` 换算并同时保留ESR=R；电感按 `L=X/(2πf)` 换算并同时保留串联电阻R；串口输出明确包含类型、参数、频率和数据来源 `8-capture-complex-average`。
- Debug全量构建无警告，RAM 7120 B、FLASH 55312 B；固件已烧录并通过Flash校验。
- 使用仍连接的1.194 kΩ电阻上板复测：自动量程3次探测后锁定100 Ω反馈/V1×/I8×，正式8/8样本VALID，平均 `1210.659+j39.734 Ω`、相位+1.880°；正确输出 `type=R, R=1210.659 ohm, f=10000 Hz`，随后自动停止激励。
- 已确认TIM6同时触发DAC和双ADC，1 kHz时64点表对应64 kS/s，1024点窗口仍恰好覆盖16个完整周期；因此现有同步DFT结构可安全用于后续1 kHz模糊区/大电容复测。

### 15. 低阻件盲测检查点

- 用户按“约10 Ω、精确值暂不公布”接入下一只电阻。自动量程起始100 Ω反馈/V1×/I1×时，V峰峰值仅约131～135 counts、I峰峰值约531～540 counts，均未削顶；算法依次提高V/I增益，3次试探后稳定锁定100 Ω反馈/V8×/I2×，激励保持1200 counts。
- 第一轮正式8次全部VALID，平均原始阻抗 `20.204+j0.289 Ω`，相位+0.820°，正确初判为电阻；第二轮独立8次也全部VALID，平均 `20.185+j0.267 Ω`，相位+0.758°。
- 两轮电阻分量只差0.019 Ω（约0.094%），因此当前盲测锁定值为约20.19 Ω，不是单次偶发结果。用户尚未公布万用表参考值，待公开后计算绝对误差并确认实际接入件/接法。
- 该DUT在最低灵敏度下没有削顶，所以本次没有实际触发DAC幅度自动降低分支；该分支仍须用更低阻抗或更大电流工况单独上板验证，不能把本次测试写成已验收。
- 用户公布万用表参考值为20.4 Ω；两轮盲测电阻分量合并中心值20.1945 Ω，偏差-0.2055 Ω，相对误差-1.007%，满足±5%指标。低阻档自动反馈/PGA选择、8次平均和电阻判别均通过该参考件交叉验证。
- 根据100 Ω反馈/I1×下20.4 Ω DUT的I峰峰值约535 counts估算，10 Ω DUT预计仍不会削顶；验证“最低灵敏度仍过大并自动降低激励”应使用约3 Ω以下的专门诊断工况。该工况超出10 Ω验收下限，只用于状态机分支测试。

### 16. 约100 Ω中低阻档盲测

- 用户接入约100 Ω、精确值暂不公布的参考件。两轮自动量程均在2次试探后锁定100 Ω反馈/V2×/I2×；锁定时V/I峰峰值均约920 counts，无削顶，处于目标窗口内。
- 第一轮正式8次全部VALID，平均原始阻抗 `100.421+j2.483 Ω`、相位+1.416°；第二轮8次也全部VALID，平均 `100.426+j2.588 Ω`、相位+1.476°。两轮均正确初判为电阻并自动停止激励。
- 两轮电阻分量仅差0.005 Ω，盲测合并中心值为100.4235 Ω；用户尚未公布万用表参考值，待公开后补充绝对/相对误差。
- 用户公布万用表参考值为99.6 Ω；盲测合并中心值偏高0.8235 Ω，相对误差+0.827%，满足±5%验收指标。该结果没有利用参考值反向拟合校准系数。

### 17. 约10 kΩ中阻档盲测与量程策略回退

- 用户接入约10 kΩ、精确值暂不公布的参考件。原自动量程在5次试探后锁定100 Ω反馈/V1×/I64×，正式8/8样本VALID，初次平均 `9829.287+j1367.067 Ω`、幅值9923.898 Ω；约+7.9°相位进入5°～10°模糊带，因此分类器正确保留UNKNOWN，没有强行判为电阻。
- 为降低高PGA相位误差，曾实验性把自动量程I增益上限限制为16×并优先切换更高反馈档。上板后1 kΩ反馈出现V/I THD约12%～26%，继续切到10 kΩ反馈后I THD稳定约22.6%，8次正式样本全部SUSPECT、`eligible=0`、平均不可用。失败样本全部保留；该策略被判定不可用并明确回退，不作为有效测量结果。
- 已恢复允许1×～128×PGA并动态枚举全部可用反馈组合的上一版策略；Debug全量构建无警告，RAM 7120 B、FLASH 55432 B，重新烧录并通过Flash校验。
- 恢复后的两轮独立测量均锁定100 Ω反馈/V1×/I64×且各8/8样本VALID：第一轮 `9879.913+j1370.289 Ω`、幅值9974.486 Ω；第二轮 `9882.649+j1365.204 Ω`、幅值9976.500 Ω。
- 两轮电阻分量仅差2.736 Ω（约0.028%）；盲测合并中心为R=9881.281 Ω、X=1367.747 Ω，幅值约9975.493 Ω。用户尚未公布万用表参考值，待公开后分别评估R分量和幅值误差。
- 当前结论：100 Ω反馈/I64×的幅值与重复性良好，但固定约+7.9°系统相位需要该频率/档位复数校准或1 kHz复测修正；不能仅为降低PGA而盲目强制切换到当前表现异常的1 kΩ/10 kΩ反馈路径。
- 用户公布万用表参考值刚好为10.000 kΩ。恢复策略后两轮合并R=9.881281 kΩ，相对误差-1.187%；复阻抗幅值约9.975493 kΩ，相对误差-0.245%，均满足±5%指标。R与幅值差异来自尚未校准的约+7.9°系统相位。

### 18. 模糊相位1 kHz自动二次测量（实现完成，待硬件验证）

- 扩展 `lcr_component.c/.h`，新增双频确认接口。确认逻辑不只看单次相位：电阻要求两频率下均R占主导、|相位|不超过10°且R变化不超过20%；电容要求降频后负电抗幅值明显增大；电感要求升频后正电抗幅值明显增大。无法满足关系时继续返回UNKNOWN。
- 测量流程现在每次新按键固定从10 kHz开始。若10 kHz的8次复数平均落入5°～10°模糊带，固件会先完整输出并保存该平均值，然后自动停止激励、切换到1 kHz、重新从安全档自动量程，再采一组新的8次平均。
- 1 kHz下仍使用64点同步表与TIM6同时触发DAC/双ADC，采样率64 kS/s；1024点窗口保持16个完整周期。双频的每条试探/正式样本和两个平均值分别输出，不覆盖、不混合，也不会因二次测量失败伪造分类。
- 双频结束后串口明确输出primary/secondary频率以及 `source=two-frequency-confirmed` 的最终R/C/L或UNKNOWN。新的人工测量会先恢复默认10 kHz；用户中止时同时清理双频确认状态。
- Debug全量构建通过且无警告，RAM 7152 B、FLASH 57580 B；固件已烧录并通过Flash校验。按用户要求本阶段先不启动硬件测量，因此1 kHz自动衔接和双频判据必须标记为待上板验证，不能写成已验收。

### 19. 统一测量终态和ADC DMA超时（实现完成，待故障注入验证）

- 新增 `lcr_workflow.c/.h`，统一记录完整测量流程状态：`IDLE`、主频自动量程/平均、复核频率自动量程/平均、`COMPLETE`、`ABORTED` 和 `ERROR`；10 kHz主测与1 kHz复核不再依靠零散布尔量隐含判断当前阶段。
- 错误码覆盖ADC启动失败、DMA超时、ADC处理失败、激励控制失败、DFT失败、自动量程失败、无DUT/开路、8次无合格样本、二次测量启动失败和二次平均无效。所有错误统一执行：中止ADC、停止激励、取消后续采样、熄灭LED并输出终态和HAL清理结果。
- 终止时不会静默掩盖数据：串口保留并报告已经完成的正式采样次数、合格/不合格次数和自动量程探测次数；尚未完成的DMA缓冲明确标记为“不参与结果”，不伪装成一次有效或无效的完整采样。
- 每次ADC DMA启动后设置随频率变化的非阻塞截止时间：`ceil(1000×1024 / (f×64)) × 4 + 20 ms`，并限制在50～1000 ms。当前10 kHz实际超时为50 ms，1 kHz为84 ms；主循环检查截止时间，没有加入阻塞延时。
- 自动量程在电流信号持续过小且已达到灵敏度/调整上限时输出 `NO_DUT_OR_OPEN`；其他换挡/应用失败输出 `AUTORANGE`。二次测量的启动或平均失败使用独立错误码，避免误报为一次正常完成。
- 正常8次平均或双频确认结束时明确输出 `state=COMPLETE; code=NONE`；用户主动停止时输出 `state=ABORTED; code=NONE`。自动量程锁定后才进入对应平均状态。
- Debug全量构建通过且无警告，RAM 7160 B、FLASH 59276 B；固件已烧录到NUCLEO-G474RE并通过Flash校验，烧录时板端电压3.31 V。
- 按用户“先不验证”的要求，本阶段未触发新的板上测量，也未人为制造DMA/自动量程故障；因此正常终态、超时和各错误码的硬件分支均记录为待上板验证，不能写成已验收。

### 20. 动态采样点数与高频激励基础配置（已烧录，待元件实测）

- 按最终验收PDF重新核对并在本文顶部固化“不可回退”基线：电阻/电容/电感三类能力必须共存，每类最终不少于7个元件、商用LCR表定值、相对误差绝对值不大于5%，并完成一键测量与屏幕显示。测通一个电容只代表本阶段检查点，不代表电容范围或最终验收完成。
- 激励模块由固定64点扩展为按频率自动选择最长可用正弦表：不超过12.5 kHz使用64点，12.5～25 kHz使用32点，25～50 kHz使用16点；总DAC/ADC同步触发率始终不超过800 kS/s。50 kHz高频档对应16点、800 kS/s，为后续10～100 µH电感提供基础。
- `LCR_ExcitationStatus`新增实际每周期点数；DAC DMA长度、TIM6实际频率换算、ADC DMA超时和DFT相位索引全部读取同一实际点数，避免高频缩短表后仍按64点计算造成频率、超时或阻抗相位错误。
- 1024点采集窗在64/32/16点三种模式下分别覆盖16/32/64个完整周期，继续满足同步DFT的整数周期要求；H1～H5分析均支持三种配置。
- 已有1 kHz、10 kHz主测/复核仍选择64点，电阻自动量程、8次平均、分类、错误状态和100 kΩ坏反馈档隔离逻辑未删除、未旁路。
- Debug全量构建通过且无警告，RAM 7160 B、FLASH 59876 B；固件已烧录并通过Flash校验，板端电压3.31 V。
- 本次尚未接入或触发电容实测，高频档也尚未做示波器/ADC上板验证；当前只记录为“基础配置已实现并烧录”，不能写成电容或电感量程已验收。
- 用户接入首只未知电容后的第一次盲测没有形成有效结果：自动量程4次试探均完整保留，最终明确输出`state=ERROR; code=AUTORANGE`并停止激励，没有伪造C值。V/I两路ADC平均值均约998 counts（约0.80 V），而模拟板正常+5 V供电时共享后级偏置应约2085 counts（约1.68 V）；按2 kΩ/1 kΩ偏置比例反推当前模拟板电源仅约2.4 V。该次记录判为供电异常下的失败样本，不用于评价电容或算法误差，待恢复J7 +5 V后对同一电容重测。
- 恢复+5 V后，同一只标称未知电容的第一轮有效盲测在4次试探后锁定100 Ω反馈/V16×/I1×；正式8/8样本VALID，平均raw `Z=0.012-j3.763 Ω`、相位-90.183°，正确识别出强电容性。但最终参数行暴露出精简版`printf`不支持`%llu`，导致64位C值之后的可变参数错位并输出乱码；该显示缺陷被明确记录，没有把损坏输出当成通过。
- 已新增不依赖`printf`长长整型支持的十进制64位输出函数，C和L参数均改用该函数，避免同类问题延伸到后续电感显示。修正版Debug全量构建无警告，RAM 7160 B、FLASH 60036 B，烧录和Flash校验通过。
- 修正版对同一未知电容再次盲测：4次试探后仍锁定100 Ω反馈/V16×/I1×，正式8/8样本VALID；平均raw `Z=0.015-j3.780 Ω`、|Z|=3.780 Ω、相位-90.227°，输出`type=C, C=4210448 pF, ESR=0.015 Ω, f=10000 Hz, source=primary-8-capture-average`，最终`state=COMPLETE; code=NONE`并自动停止激励。
- 两轮有效复阻抗换算的C约为4.229 µF和4.210 µF，重复差约0.45%；用户尚未公布该电容的商用LCR表/标称参考值，因此当前只确认自动量程、复数平均、C识别与参数显示链路工作，绝对误差仍待参考值公开后计算，不能提前写成≤5%验收通过。
- 用户公布该电容标称值为4.7 µF。修正版盲测4.210448 µF相对标称值偏差-10.416%，第一轮由有效复阻抗换算的约4.229 µF也与标称相差约-10.0%；该差异超过最终±5%指标，但4.7 µF器件自身容差未知，标称值不能代替验收PDF要求的商用LCR表真值。阶段结论严格限定为“单电容自动测量、分类、C/ESR及R±jX输出链路通过；电容绝对精度未验收”，后续多点校准时必须回到相同频率和等效模式重新比较。

### 21. 统一最终结果快照（已实现并烧录，OLED已接入消费）

- 新增`lcr_result.c/.h`，把原先只存在于即时串口流程中的最终信息集中成稳定快照：测量序号、运行/完成/中止/错误状态、错误码、主频和次频的独立测量记录、自动量程探测数、attempt/eligible/ineligible、复阻抗平均值、校准来源以及最终R/C/L分类参数。
- 每次新测量先生成新的sequence并清空旧内容，避免OLED误显示上一次结果；10 kHz平均和可选二次频率平均分别保存在`primary`与`secondary`，不覆盖、不混合。
- 正常单频分类或双频确认后保存`COMPLETE`和元件参数；用户中止保存`ABORTED`及当时已经完成的统计；ADC、DFT、自动量程或确认失败保存`ERROR`和相应错误码。终态仍继续输出完整原始串口日志，并额外打印一行快照摘要。
- 错误/中止路径在取消测量会话前先复制已完成的上下文；未完成的DMA样本仍明确排除，满足“不能静默丢弃或伪造结果”的既有要求。
- 该模块只汇总现有结果，没有更改阻抗计算、自动量程、质量门限、R/C/L分类、100 kΩ坏档隔离或电阻/电容路径，后续OLED和Flash校准将读取同一快照，避免各自重复拼装结果。
- Debug全量构建通过且无警告，RAM 7304 B、FLASH 61056 B；固件已烧录并通过Flash校验，板端电压3.31 V。按本阶段约定未额外触发元件测量，因此新快照串口摘要及RAM内容仍标记为待上板流程验证。

### 22. OLED I2C安全探测（已上板，重新接线后发现0x3C）

- 新增`lcr_display.c/.h`的只读地址探测接口，启动时只检查OLED最常见的7位地址0x3C和0x3D；在控制器与分辨率未知时不发送初始化/显存命令，避免把SSD1306参数错误写给SH1106或其他设备。
- 无论显示是否存在，测量、串口和激励初始化均继续运行；未发现设备时明确输出`display remains disabled`，不会因OLED缺失阻塞核心LCR功能。
- Debug全量构建通过且无警告，RAM 7304 B、FLASH 62696 B；固件烧录和Flash校验成功，板端电压3.31 V。
- 上板复位后串口实测输出`OLED I2C probe: no response at 0x3C or 0x3D`；其余安全启动信息正常，激励保持停止。当前结论仅为“PB8/PB9上的这两个地址未应答”，不能凭此推断OLED型号，也不能写成显示硬件或驱动已完成。
- 下一步需确认OLED是否已经供电并连接到PB8/SCL、PB9/SDA、GND，取得模块控制器与分辨率信息；若确认已连接仍无应答，再扩大为全7位地址扫描并检查I2C电气状态。
- 用户重新接线后再次复位探测，串口稳定输出`OLED I2C candidate detected at 7-bit address 0x3C`；确认OLED供电及PB8/SCL、PB9/SDA通信链路已经连通。用户提供模块信息为0.96英寸、`O96W4-small`、4针；结合0x3C只能把SSD1306/128×64作为高概率候选，控制器和显示方向仍须通过受控初始化与测试画面确认，不能仅凭外形写成已确认。

### 23. SSD1306候选初始化与128×64测试图（通信及目视均通过）

- 在0x3C地址实际应答和用户提供0.96英寸4针模块信息的基础上，按SSD1306 128×64候选配置扩展`lcr_display`：显示关闭期间设置时钟、1/64复用、偏移、内部电荷泵、水平寻址、SEG/COM方向、COM硬件配置、对比度、预充电、VCOMH、正常显示，最后再开屏。
- 初始化只在地址应答后进行；任意I2C写失败时显示保持不可用，但LCR测量和串口继续运行，不让显示外设成为核心测量的单点故障。
- 新增128×64、8页写入路径以及对称测试图：四周单像素边框，内部两条对角线形成大X。图案对水平/垂直翻转对称，可优先验证64行、128列寻址完整性，不把方向问题与控制器不兼容混在一起。
- Debug全量构建通过且无警告，RAM 7304 B、FLASH 64216 B；固件烧录并通过Flash校验，板端电压3.31 V。
- 上板复位串口实测：`OLED candidate at 0x3C: SSD1306 128x64 init HAL=0, test pattern HAL=0`，说明模块对完整初始化命令及8页显存数据均完成I2C应答。用户随后目视确认边框和大X测试图显示“非常合适”，因此128×64寻址、方向和基本像素输出完成本阶段目视检查；该结论只覆盖显示硬件/测试图，不等同于测量结果页面已验收。

### 24. OLED正式状态与结果界面（已实现、编译和烧录，待结果页实测）

- `lcr_display`新增1024字节128×64帧缓冲、5×7 ASCII字库、8行文本绘制及整屏刷新；未改变已通过目视验证的SSD1306初始化参数。显示写入失败只记录HAL状态，不阻断激励、ADC、自动量程或串口诊断。
- 上电测试图已替换为正式待机页：`LCR METER / READY / PRESS BUTTON / R C L AUTO RANGE`。按键启动后显示10 kHz测量中页面；若进入模糊相位二次确认，页面同步切换为实际1 kHz；用户主动中止、测量错误和正常完成分别显示独立终态页面。
- 正常结果页直接读取统一`LCR_FinalResult`快照，不另算一套数据：同时显示测量序号、`Z=R±jX`、R/C/L或UNKNOWN类型、实际频率、对应R/C/L参数、电容ESR或电感串联电阻、合格/尝试样本数及RAW/CALIBRATED来源。双频确认时优先选择与最终参数频率一致的主/次频阻抗，保证屏幕上的Z、参数和频率属于同一测量条件；错误页保留错误码、样本数和自动量程探测数，未完成数据不冒充结果。
- 电容按pF/nF/µF工程单位显示，电感按nH/µH/mH显示；OLED仅有ASCII字库，因此µ在屏幕上明确写成`U`。64位C/L参数继续使用整数拆分，未重新引入精简`printf`不支持`%llu`造成的参数错位问题。
- Debug全量构建通过且无警告，最终RAM 8328 B、FLASH 68972 B；固件已烧录并通过Flash校验，板端电压3.31 V。复位串口实测输出`OLED at 0x3C: SSD1306 128x64 init HAL=0, ready screen HAL=0`，其余安全启动、100 kΩ坏反馈档隔离和测量配置日志正常。
- 本次只复位验证待机页I2C写入，没有额外触发DUT测量；用户随后目视确认正式`READY`待机页显示正常。测量中页、真实R/C结果页以及错误/中止页仍属待上板流程验证；电感实测继续按用户要求暂缓，不能把L结果页代码存在写成电感验收完成。

### 25. 2026-07-19断电交接快照

- 当前固件是Stage 24正式OLED界面版，已编译、烧录、Flash校验并完成上电日志检查。最后一次确认状态为OLED 0x3C初始化成功、`READY`页目视正常、DAC/ADC激励停止；用户计划断电，下一次上电会重新进入同一`READY`页。
- NUCLEO与LCR模拟板信号接线：Arduino A2/PA4/DAC1_OUT1 → LCR板J3/SINE；LCR板J1/V_AMP → Arduino A0/PA0/ADC1_IN1；LCR板J2/I_AMP → Arduino A1/PA1/ADC2_IN2；两板GND必须共地。J2不是正弦输出口，原始正弦输出检查点是A2/PA4，进入模拟板的接口是J3/SINE。
- 模拟板电源：J7的+5 V必须接稳定5 V电源，J7 GND接公共地；曾经的约2.3～2.4 V欠压会使V/I偏置降到约0.8 V并导致自动量程失败，恢复5 V后正常偏置约1.67～1.68 V。明天若出现两路均异常，应先检查J7供电，不先改算法。
- DUT使用J6四线端子：1=DUT+、2=DUT+SENSE、3=DUT-SENSE、4=DUT-；测试件按对应Force/Sense连接，不能只形成Sense连接而缺少DUT+到DUT-的电流回路。
- OLED为0.96英寸`O96W4-small`四针模块：VCC → NUCLEO 3.3 V，GND → 公共地，SCL → PB8/Arduino D15，SDA → PB9/Arduino D14，I2C 7位地址实测0x3C；该完整四线接法已经通过测试图和正式`READY`页目视验证。
- 板载控制：蓝色`B1 USER`为PC13测量键，短按一次启动；黑色`RESET`只复位固件并回到`READY`，不是测量键。一次测量自动完成量程试探、8次采样及必要的1 kHz二次确认，正常/错误结束都会自动停止激励；测量中再次按蓝色键会主动中止。
- 已知硬件缺陷继续保留并显式处理：ADG1609 code00的100 kΩ反馈支路仍判定为坏档，正常自动量程会隔离该档并使用健康档位替代；没有删除该支路定义，待用户修复后必须重新诊断、解除隔离并重新校准，不能直接恢复旧系数。
- 已经完成的开发检查点包括10 Ω级、100 Ω级、约1.194 kΩ、10 kΩ和约93.7 kΩ电阻链路，以及单只4.7 µF标称电容的自动测量/分类链路；这些不是最终验收。电容曾测得约4.210448 µF，和4.7 µF标称值相差约-10.4%，器件真实值尚缺商用LCR表同频同模式参考，绝对精度未通过验收。
- 明天第一项不是重新烧录：保持上述接线，上电确认`READY`，先接已知约10 kΩ电阻，短按一次蓝色USER键，核对`MEASURING`→`RESULT`页面和串口快照；通过后再对已放电的4.7 µF电容做同样检查。今天没有把这两次正式OLED结果页验证记录为已完成。

### 26. 验收测量记录表（已生成，待填实测数据）

- 新增`outputs/lcr_acceptance_20260720/LCR验收测量记录.xlsx`，使用单张平面数据表预留电阻、电容、电感各7条，共21条验收记录；标称点覆盖10 Ω～100 kΩ、1 nF～4.7 µF和10～100 µH范围，标称点可按实际元件修改。
- 字段包括参考仪器型号、测试日期、测试人员、元件类型、标称值、商用LCR标准值、本机测量值、相对误差、测量频率、等效模式、本机R/X分量和备注。按用户要求没有“通过判定”列。
- 相对误差使用表内公式`|本机值-标准值|/|标准值|`，标准值或本机值为空时保持空白；等效模式提供串联/并联下拉框，默认串联。标准值必须来自商用LCR表在与本机相同频率、相同等效模式下的结果。
- 已检查21条记录、公式和空值行为，未发现`#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`或`#N/A`；整表完成视觉渲染检查，中文表头、输入栏和全部记录无截断。当前仅为模板，任何空白行都不能记录为已测或已验收。

### 27. 首批范围摸底数据（用户已填表，电容需分段诊断）

- 用户在`LCR验收测量记录.xlsx`中填入当前能完成的测量。7个电阻点分别以万用表20.2 Ω、99.6 Ω、1.194 kΩ、10.0 kΩ、39.7 kΩ、67.0 kΩ、93.9 kΩ为参考，本机结果20.169 Ω、100.751 Ω、1.194 kΩ、9.982 kΩ、40.838 kΩ、69.732 kΩ、97.384 kΩ；相对偏差约-0.153%、+1.156%、0%、-0.180%、+2.866%、+4.078%、+3.710%，全部落在±5%内。该结论仅是相对万用表的开发摸底，不替代最终要求的商用LCR表同频同模式参考。
- 电容当前没有商用LCR标准值，只能暂时与标称值比较：220 nF→228.651 nF（+3.932%），470 nF→448.817 nF（-4.507%），1 µF→871.175 nF（-12.883%），4.7 µF→4223 nF（-10.149%）。220/470 nF接近标称，而1/4.7 µF明显系统性偏低，不能用单一全范围比例系数修正，否则会破坏中档结果。
- 1 nF和10 nF均记录为`ERROR`，100 nF行尚未形成有效记录；错误码、自动量程最终档位、PGA、质量原因和串口原始Z尚未写入表中，因此目前不能把小电容失败简单归因于灵敏度、100 kΩ坏反馈档或分类逻辑中的任何一个，必须先取得完整串口证据。
- 现有4个有效电容点的C值与10 kHz下记录的X分量严格一致，说明显示换算公式自身没有明显错位：问题更可能位于分档后的复数增益/相位标定、模拟前端频率响应或量程选择，而不是nF/µF单位显示错误。下一步应各选一个失败点（优先10 nF）和一个偏低点（优先1 µF）抓完整串口流程，再决定是修自动量程、分档校准还是模拟链路。
- 用户把标准值列改为万用表值并以`k`后缀文本录入部分电阻，同时电容标准值填`无`，原有相对误差公式列已无公式/计算结果。当前不擅自改动用户实测表；最终验收前应把商用LCR结果按纯数值、与本机相同单位填入，才能让相对误差列自动计算并作为可审计数据。
- 电感7行均记录“无器材”，与用户此前暂缓电感实测一致；这仍是最终验收缺项，不能视为完成。

### 28. 电容谐波自适应质量判据（10 nF误杀已修复并实测通过）

- 修复前对同一只10 nF完整抓取串口：自动量程4次后稳定锁定100 Ω反馈、V1×、I8×，V/I峰峰值均处于窗口内；8次正式raw Z稳定约`4～18 Ω-j1530～1545 Ω`，但I THD均为17.192%～18.553%，超过原固定10%门限，导致8/8全部`SUSPECT`、最终`NO_ELIGIBLE_SAMPLES`。因此ERROR根因不是信号太小或硬件测不到，而是质量门限误杀。
- 同批数据中V THD约7.4%～7.8%；对电容而言电流是电压的微分，第n次电流谐波相对基波应约为电压对应比例的n倍。由实测V H2～H5计算出的理想电容I THD约17%～18%，与实际I THD高度一致，证明这部分谐波是对现有激励波形的物理响应，不能沿用电阻的固定10%电流THD门限。
- 质量判据新增有界自适应策略：只有`V-I`相位位于+45°～+135°、明确呈电容特征时，才按`sqrt(sum((n×V_Hn/H1)^2)) + 3%`推导I THD上限，并限制在10%～30%；电阻及电感仍保留原10%上限，避免放过此前会造成100 kΩ电阻离群值的16%异常THD。串口逐样本公开实际动态上限，不静默放宽。
- 修正版Debug全量构建通过且无警告，RAM 8328 B、FLASH 69392 B；固件烧录并通过Flash校验，板端电压3.31 V。
- 同一10 nF原位复测：4次量程探测后仍锁定100 Ω/V1×/I8×；8次正式样本I THD 17.282%～18.164%，对应动态上限19.927%～21.094%，全部`VALID`。平均raw `Z=7.704-j1534.480 Ω`、相位-89.712°，输出`type=C, C=10372 pF, ESR=7.704 Ω, f=10000 Hz`，最终`state=COMPLETE; code=NONE`并自动停止激励。
- 10.372 nF相对10 nF标称值约+3.72%，属于开发期较好结果，但仍缺商用LCR表同频同模式真值，不能写成最终±5%验收通过；当前档位也明确输出“无校准点”，结果来源仍为raw。OLED结果页已由固件成功刷新，实际页面内容仍待用户目视确认。

### 29. “1 µF”复测发现约1000倍量级不一致（暂停算法修改，待核对元件/接线）

- 用户按1 µF偏低点复测流程接入元件。修正版质量判据正常工作，自动量程6次后锁定100 Ω反馈、V1×、I128×，8/8正式样本全部`VALID`，证明Stage 28修改没有造成流程错误。
- 8次raw Z非常稳定，最终平均`Z=3856.930-j17920.750 Ω`、相位-77.854°，固件换算`C=888 pF`、ESR=3856.930 Ω并正常`COMPLETE`。10 kHz下理想1 µF容抗约15.9 Ω，而实测X约17.9 kΩ，对应约0.888 nF，量级相差约1000倍；这不是此前1 µF测得871 nF的约-13%比例偏差。
- 自动量程需要升至I128×且I基波仍仅约630 counts，也与约1 nF级小电流相符。当前证据优先指向接入了1 nF/标识混淆、DUT四线接触未真正夹住1 µF而只测到寄生电容，或元件本身错误；不能用校准系数解释，更不能据此继续改阻抗算法。
- 当前暂停固件修改，需用户确认元件表面标识（常见`102`约为1 nF、`105`约为1 µF）并检查J6的DUT+/DUT+SENSE与DUT-/DUT-SENSE均实际接触。确认后重新测同一目标1 µF，再与Stage 27的871.175 nF数据比较。
- Stage 28只在明确电容相位时调整样本质量门限，不改阻抗值；电阻仍保留固定10% I THD门限。为完整排除回归，电容量级核对完成后仍安排约10 kΩ电阻做一次实测回归，不仅依赖代码路径分析。

### 30. 外部硬件接线还原记录补全

- 按“完全拆线后只看记录即可逐根还原”的标准复查，原Stage 25已完整记录模拟三线、J7供电、J6顺序和OLED，但7根量程/PGA控制线只有MCU信号名，缺少LCR板J5针号；J7重复电源针、同轴接口信号/屏蔽针、普通两脚DUT如何并接四线端子、NUCLEO连接器针号和板载虚拟串口也不够明确，因此没有把原记录冒充为完整。
- 新增`.agents/hardware_wiring.md`作为唯一完整还原清单：逐根列出3根模拟信号、7根J5控制线、J7-1/2为+5V和J7-3/4为GND、OLED四线、J6 Kelvin/普通两线接法、NUCLEO CN1/CN5/CN6/CN8/CN9针号、按键、板载ST-LINK虚拟串口及上电自检。
- 初版补全时曾把可用的NUCLEO CN6-5供电方案误写成当前标准接法；用户随后提供原接线快照，明确实际使用的是独立外部5V。该差异不静默覆盖，见Stage 31更正。OLED仍只接3.3V，J7禁止接3.3V。
- 本阶段只补文档，没有编译、烧录或改变板上固件。此前写入工作区但尚未构建/烧录的电容插值草稿也没有继续推进，等待用户确认标定方案。

### 31. J7实际供电来源更正（用户接线快照确认）

- 用户提供的原接线快照确认：`外部5V → LCR J7 +5V`，同时`LCR GND → NUCLEO GND`；3根模拟线和J5-1～J5-7的全部控制线与`hardware_wiring.md`一致。
- 已把唯一还原清单的标准接法改为：外部稳压5V正极接J7-1或J7-2，负极接J7-3或J7-4，外部电源负极、LCR板GND、NUCLEO GND三者共地。明确当前实际接法不使用NUCLEO CN6-5给J7供电，也禁止把两路5V并联。
- 本次只更正接线文档，没有改动、构建或烧录固件。

## 下一步

1. 先核对当前“1 µF”元件表面标识和J6四线接触；当前实测只对应约0.888 nF，量级未对上，禁止继续按1 µF精度分析。确认真正1 µF接入后复测，再进行约10 kΩ电阻回归。

### 32. 100 kΩ反馈支路重焊后的软件恢复与首轮自检（2026-07-22）

- 用户确认100 kΩ反馈支路已经重新焊接。已移除100 kΩ档不可用表、100 kΩ请求自动映射到10 kΩ的临时回退，以及自动量程跳过100 kΩ候选的逻辑；`LCR_SelectFeedbackRange(100K)`现在保持requested/actual均为100 kΩ，`feedback_fallback_active=false`。
- 自动量程仍从100 Ω、V/I增益1倍的安全低跨阻状态开始，但搜索候选已经恢复为100 kΩ、10 kΩ、1 kΩ、100 Ω四档；未改其他量程阈值、搜索策略、校准模型或显示逻辑。
- 恢复10 kHz、100 kΩ反馈、V/I增益1倍的100 kΩ专属临时校准点；阻抗计算和校准均按实际100 kΩ反馈网络选择，禁止以10 kΩ参数替代。
- 新增上电一次性100 kΩ硬件诊断：先停止激励并强制100 kΩ/1倍/1倍，核对真实映射，启动激励后检查I通道交流响应；异常只报错并恢复安全100 Ω状态，不回退到10 kΩ。自检通过后才连续执行5次93.7 kΩ强制验证，并逐次打印V/I峰峰值、THD、raw R/X、校准R/X及相对误差。
- Debug全量清理构建通过：RAM 8328 B，FLASH 73364 B；已用ST-LINK烧录并完成Flash校验。
- 首轮真实上板自检结果：V p2p=597，I p2p=4095，I H1=2530.860 counts，I THD=36.573%。I通道不再是此前p2p=0的固定直流，但当前发生全量程削顶，不能判为正常100 kΩ响应；固件已明确报告ERROR、停止激励并恢复100 Ω，未执行或伪造93.7 kΩ的5次通过结果，也未静默改用10 kΩ。
- 当前待验证：可靠接入约93.7 kΩ参考电阻后复位，取得5次强制100 kΩ数据并确认误差/稳定性不超过5%；随后换接1 nF电容，通过普通按键自动量程日志确认其能够选择100 kΩ档。验证未完成前保留真实故障状态。
- 接入约93.7 kΩ后，100 kΩ物理支路自检连续两次通过：首组I p2p=420、H1=81.815 counts，次组I p2p=303、H1=77.191 counts；此前固定直流/p2p=0故障未复现，requested/actual均为真实100 kΩ。
- 第一组5次强制100 kΩ校准R为91.700、95.499、98.352、93.690、87.194 kΩ，其中4/5相对93.7 kΩ误差不超过5%，组内跨度约11.9%，失败；第二组为91.497、96.213、93.018、95.704、89.730 kΩ，5/5单次误差不超过5%，但组内跨度约6.9%，仍未满足5%稳定性判据。两组I THD多在约11%～17%，故100 kΩ支路判定为“交流响应已恢复、精度稳定性未验收”，没有修改校准模型掩盖波动。
- 换接1 nF后，强制100 kΩ自检通过：I p2p=905、H1=327.162 counts、THD=6.029%；普通自动量程从100 Ω/1倍开始，经过7次探测后锁定100 Ω反馈、I增益64倍，而不是100 kΩ。锁档时V p2p=2016、I p2p=1113，已进入820～3276目标窗，现有策略按设计立即锁定。8次正式采集中7次合格，最终为988 pF、ESR 9.879 Ω。
- 因用户限定本阶段不得顺带修改其他量程算法，本阶段没有为了让1 nF强行选择100 kΩ而改变灵敏度搜索或锁档规则；因此“1 nF自动选择100 kΩ”验收项真实记录为失败。若后续授权专项调整，需要评估当前按`Rf×PGA`排序而未计入100 kΩ支路并联电容在10 kHz下有效反馈阻抗的影响。
- 93.7 kΩ专用5次验证入口保留为显式参数，不再在日常上电自动执行；最终烧录固件上电只运行一次100 kΩ硬件自检。用当前1 nF复核最终固件：V p2p=2055、I p2p=986、I H1=340.821 counts、I THD=8.924%，自检PASS，随后停止激励并恢复安全100 Ω档。
- 最终Debug和Release均全量清理构建通过，无编译错误；Debug RAM 8328 B、FLASH 73404 B，Release RAM 8320 B、FLASH 48532 B。最终Debug固件已烧录并完成Flash校验。
2. 显示流程确认后，实现校准数据版本、有效性检查和Flash保存，为后续使用商用LCR表进行多点校准做准备；现有电阻、电容、自动量程和错误记录能力不得回退。
3. 最终仍需按验收PDF完成R/C/L三类各不少于7个元件、与商用LCR表同频率同等效模式对照及绝对相对误差≤5%；电感实测按用户当前要求暂缓，但缺项持续明确记录。

### 33. 自动量程、校准与有效样本统计修正（2026-07-22）

- 本阶段仅修改`lcr_autorange.c/.h`、`lcr_calibration.c`、`lcr_measurement.c/.h`、`lcr_component.c`、`lcr_range.c/.h`、`lcr_result.c`和`main.c`中的量程、校准、统计及双频复核衔接；未修改GPIO映射、ADC双模DMA、DAC/TIM6、阻抗基础公式或OLED显示实现。
- 自动量程灵敏度从错误的`Rf×I增益`改为按当前实际频率计算：`Cf=capacitance_tenths_pf×1e-13`、`omega_rc=2πfRfCf`、`|Zf|=Rf/sqrt(1+omega_rc²)`、`sensitivity=|Zf|×I增益`。搜索、目标倍数、升降档和边界候选统一使用该值。10 kHz下四档1倍有效灵敏度约为100 kΩ档7215、10 kΩ档9906、1 kΩ档1000、100 Ω档100；1 kHz下约为58613、9999、1000、100，四档均在候选循环中，无100 kΩ回退、跳过或重映射字段/逻辑。
- 自动量程锁定改为同时满足：V/I无近轨或p2p削顶、V/I H1有效、H1幅值在410～1638 counts目标窗、V THD及按既有电容谐波模型计算的I THD合格。p2p只用于近轨/削顶和低于410 counts的辅助弱信号判断；若H1已在窗口但THD不合格则降低对应通道灵敏度，无法找到合格配置则真实失败。
- 已彻底删除7个电容标称值插值表及`LCR_CalibrationApplyCapacitanceCurve()`。校准只按`frequency + feedback_range + voltage_gain + current_gain`精确匹配复数系数`K=a+jb`；缺点时保留并明确输出`RAW/UNCALIBRATED`。当前仅有两个临时点：10 kHz + 100 kΩ + V1× + I1×，以及10 kHz + 10 kΩ + V1× + I8×；其余频率/反馈档/V增益/I增益组合全部仍缺硬件校准点。
- 正式测量改为最多16次尝试，质量无效样本只增加attempt/ineligible、不增加eligible；收集到至少5个有效复数Z即停止，16次仍不足5个则失败。自动量程探测样本仍完全排除，锁档后等待10 ms再开始全新的正式采样。
- 对有效Z先分别取得R/X中位数，以样本相对中位数的R/X最大偏差作为复数距离；剔除超过`max(3×中位距离, 中位阻抗幅值的2%)`的离群样本，至少保留3个后分别平均R和X，再重新计算|Z|、相位及后续C/L。汇总日志新增retained和outliers计数。
- 主测保持10 kHz，只有主测分类UNKNOWN才启动1 kHz复核。双频不再直接平均阻抗，也删除电抗变化1.5倍判据；现在分别由两次Z计算C或L，仅当两个C或两个L相差不超过10%时用10 kHz主测参数确认类型，否则保持UNKNOWN。电阻双频检查也保留10 kHz主测R，不再平均两个频率的R。
- Debug与Release均全量清理构建通过且无编译错误，去除旧电阻值平手判定后再次增量链接通过：最终Debug RAM 8640 B、FLASH 75708 B；Release RAM 8632 B、FLASH 49752 B。`git diff --check`通过；本阶段按要求只编译，尚未烧录或进行新算法上板回归。

### 34. 47 µH 电感一键测量链路恢复（2026-07-22）

- 修复电感THD自适应改动遗留的调用参数不完整问题：`LCR_EvaluateCaptureQuality()`与`LCR_PrintCaptureQuality()`调用点现同时传递电压、电流动态THD门限；此前工作区源码因此无法生成新固件，板上仍运行旧版本。
- Debug全量清理构建通过且无警告：RAM 8640 B、FLASH 76164 B；`git diff --check`通过。固件已用ST-LINK烧录并完成Flash校验，板端电压3.31 V。
- 接入标称47 µH后短按USER键，25 kHz自动量程经4次探测锁定100 Ω反馈、V增益16×、I增益1×；正式测量5/5样本有效、无离群剔除，流程终态为`COMPLETE/NONE`。
- 实测平均原始阻抗为`2.066+j6.108 Ω`，相位71.312°；成功分类为电感并输出`L=38.885 µH`、串联电阻2.066 Ω。说明按键、自动量程、有效样本平均、L分类、串口/OLED结果快照链路已经打通。
- 当前结果是`RAW/UNCALIBRATED`；相对47 µH标称值约低17.3%。标称值不能替代同频率商用LCR表真值，电感绝对精度仍未通过±5%验收，下一步需取得该器件在25 kHz串联模式下的参考值后建立对应频率/档位校准。

### 35. 25 kHz电感两点临时校准（2026-07-22）

- 用户明确要求当前先把元件标称值作为准确标定值。通过SWD从上一轮100 µH结果快照取得精确原始数据：25 kHz、3次自动量程探测、5/5有效，原始阻抗约`4.318+j14.054 Ω`，固件换算`89.599 µH`；47 µH点此前原始换算为`38.885 µH`。
- 由`38.885 µH -> 47 µH`和`89.599 µH -> 100 µH`拟合临时传递式：`Lcal=1.04507631028907×Lraw+6.362207674 µH`。实现位于`lcr_calibration.c`，只在25 kHz、正电抗且相位至少+10°时应用；电阻、电容、1 kHz、10 kHz及非电感结果不受影响。
- 校准直接修正电抗X并重新计算阻抗幅值、相位，因此屏幕上的Z、L和频率保持数学一致；串联电阻R保留原始测量值。串口新增`PROVISIONAL_25KHZ_INDUCTANCE_2POINT`来源标识，并明确两个参考是47 µH、100 µH标称值。
- Debug与Release全量清理构建均通过且无警告：Debug RAM 8640 B、FLASH 78404 B；Release RAM 8632 B、FLASH 52144 B。Debug固件已烧录并完成Flash校验，板端电压3.31 V。
- 当前100 µH上板复测5/5样本有效、无离群剔除，平均校准阻抗`4.254+j15.692 Ω`、相位74.832°，最终`L=99.898 µH`，状态`COMPLETE/NONE`且结果标记`CALIBRATED`；相对当前采用的100 µH标定值误差约-0.102%。
- 该模型仅由两个标称点建立，47～100 µH区间外仍属于外推；后续仍应补10、22、33、68 µH等点，并最终用同频率、串联模式的商用LCR表读数替换标称值后再判断±5%验收。

### 36. 10 kHz粗测转50 kHz电感专用流程（2026-07-22）

- 按用户提供的优化方向，将一键主测恢复为10 kHz。10 kHz平均结果只要满足正电抗不小于0.2 Ω、相位不小于+3°且|Z|不超过100 Ω，即作为10～100 µH疑似电感；随后停止激励并切换50 kHz，重新从100 Ω反馈、V1×、I1×安全状态自动量程。其他R/C及UNKNOWN→1 kHz复核流程保留。
- 50 kHz自动选择16点正弦表，DAC/ADC同步触发率为800 kS/s；每次仍采1024点。自动量程在50 kHz只枚举100 Ω和1 kΩ反馈档，灵敏度继续按实际频率下`|Zf|×I增益`计算；锁档仍要求H1有效、无近轨/削顶、基波进入目标窗且动态THD合格。
- 正式测量保持至少5个有效样本、最多16次尝试，并沿用复数R/X中位数距离离群剔除后分别平均R/X的实现。50 kHz平均值单独计算`Ls=X/(2πf)`和`Rs=R`作为最终结果；10 kHz粗测Z不与50 kHz Z混合或平均。
- 新增`lcr_compensation.c/.h`及严格按`frequency + feedback + V gain + I gain`匹配的50 kHz短路复数补偿入口；当前真实短路数据尚未采集，补偿点数量保持0，缺失时明确输出`UNAVAILABLE`并保留原始Z，绝不以零值冒充已补偿。现有复数校准表已天然支持相同四维键的50 kHz独立系数。
- 磁盘文件`outputs/lcr_acceptance_20260720/LCR验收测量记录.xlsx`已用表格工具读取并渲染核对；其中电感15～21行的LCR值、频率、R、X仍为空且备注为“无器材”，与用户所述已有5个数据不一致。需用户保存当前Excel或提供实际副本后才能分析另外4个点，不能猜测填表。
- 当前板上实际为47 µH。未校准的两阶段测量成功：10 kHz粗测约`1.686+j2.575 Ω`；50 kHz正式5/5有效，100 Ω反馈、V16×、I2×，原始`3.024+j11.690 Ω`、`37.210 µH`。
- 以用户授权的47 µH标称值为当前参考，为上述精确50 kHz/100 Ω/V16×/I2×组合加入临时复数系数`K=1.2465861+j0.06378755`；不允许套用到其他PGA或反馈档。烧录后复测50 kHz校准阻抗`3.002+j14.738 Ω`，最终`L=46.913 µH`，相对47 µH约-0.185%，5/5有效并标记`CALIBRATED`。
- 最终Debug与Release全量清理构建均通过且无警告：Debug RAM 8640 B、FLASH 80204 B；Release RAM 8632 B、FLASH 53216 B。最终Debug固件已烧录并完成Flash校验，板端电压3.31 V。

### 37. 50 kHz档位独立校准继续扩展（2026-07-22）

- 用户截图确认50 kHz五点结果：10→6.700 µH、22→17.329 µH、33→36.055 µH、47→46.846 µH、100→85.409 µH；误差分别约-33.00%、-21.23%、+9.26%、-0.33%、-14.59%。误差改变符号且不单调，不能使用一个全范围比例系数。
- 从MCU快照读取100 µH实际组合为100 Ω/V8×/I2×，原始`5.268+j26.832 Ω`；加入该组合专用系数`K=1.1644973+j0.03229620`后，复测`L=99.895 µH`，误差约-0.105%，5/5有效。
- 从MCU快照读取22 µH实际组合为100 Ω/V32×/I2×，原始`2.273+j5.343 Ω`；加入该组合专用系数`K=1.2485754+j0.10574807`后，复测约`2.265+j6.922 Ω`、`L=22.033 µH`，误差约+0.150%，5/5有效。
- 当前已覆盖100 Ω反馈/I2×下的V8×、V16×、V32×三个50 kHz组合；10 µH与33 µH仍需重新接入读取实际PGA。若命中已有组合必须先验证复用效果，只有命中新组合才新增系数。
- 22 µH版本Debug全量清理构建无警告：RAM 8640 B、FLASH 80236 B；已烧录并完成Flash校验，板端电压3.31 V。

### 38. 50 kHz电感最多5个硬件profile与复数仿射校正（2026-07-22）

- 50 kHz电感正式测量现固定100 Ω反馈，启动档固定V8×/I2×；V增益只允许8×、16×、32×、64×，I增益通常保持2×。仅当I2×削顶或THD不合格时允许降到I1×，且I1×只允许与V64×组成第五个低电感profile。低于50 kHz的普通R/C自动量程仍可使用原有四个反馈档和全部PGA范围。
- 锁档前调用`LCR_CalibrationIsInductorProfileSupported()`核对最终组合，50 kHz不会再锁到1 kΩ反馈或任意未定义PGA组合。允许组合总数固定为5：100 Ω/V8×/I2×、V16×/I2×、V32×/I2×、V64×/I2×、V64×/I1×。
- 50 kHz校准数据结构已合并短路复数偏置和复数比例系数，统一执行`Zcorrected=(Zraw-Zshort)*K`，因此实部R和虚部X同时校正；随后串联模式显示`Rs=R`、`X`和`Ls=X/(2πf)`。原独立短路补偿模块及重复的两次应用路径已移除。
- 现有V8×、V16×、V32×三点仍保留此前按100/47/22 µH标称值反推的临时K，Zshort暂为0，只为维持当前调试行为；注释明确要求在声称未知阻抗精度前，必须用同profile的50 kHz四线短路和精密电阻替换。V64×/I2×和V64×/I1×保持`ready=false`，不会用单位系数冒充校准。
- Debug和Release均完成全量清理构建且无编译错误：Debug RAM 8640 B、FLASH 79852 B；Release RAM 8632 B、FLASH 53144 B。`git diff --check`通过。Debug固件已通过ST-LINK烧录、Flash校验并复位，板端电压3.31 V。
- 烧录后对当前连接件运行一次完整流程：10 kHz粗测5/5有效，约`0.885+j0.559 Ω`；50 kHz正式测量自动锁定100 Ω/V64×/I2×，3次探测、5/5有效，RAW平均约`1.884+j2.066 Ω`，对应约6.576 µH。该组合正是允许的第四profile，但尚无真实短路/电阻校准数据，因此结果保持RAW/UNCALIBRATED，验证了“正确选档但不伪造精度”的边界。
